import { model } from "./modelFactory.js";
export default model("reports", ["reporter_id", "reported_user_id", "tutor_post_id", "student_request_id", "category", "description", "status", "admin_notes", "resolved_by", "resolved_at"]);

